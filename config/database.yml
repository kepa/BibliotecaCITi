# PostgreSQL. Versions 8.2 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On OS X with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
development:
  adapter: postgresql
  encoding: unicode
  database: bib_citi_dev
  host: localhost
  username: sacul
  pool: 5

production:
  adapter: postgresql
  encoding: unicode
  database: bib_citi
  host: localhost
  username: sacul
  pool: 5

 
